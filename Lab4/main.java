/**
Lab4: Render playing card images in a 4x13 grid, with a button to shuffle.

*/
import javax.swing.*;

public class CardShufflerUI {
    // Define Class Variables
    
    // Define Class Functions

    /**
     * CardShufflerUI()
     * Usage: Constructor Function for objects of type CardShufflerUI
     * Given no parameters, create a new CardShufflerUI
     * Returns Nothing
     * Usage: sampleDeckUI = new CardShufflerUI()
     */
    public CardShufflerUI() {
        
        // Create the UI Window
    }
    
    /**
     * shuffle()
     * Usage: Randomize the order of cards within a deck of cards.
     * Given no parameters, randomize the caller's card order.
     * Returns Nothing.
     * Usage: sampleDeck.shuffle() // Shuffles sampleDeck, changing its order.
     */
    public void shuffle() {

        // Randomize the order of the cards within the deck.
        // Card Deck Data Type: #### AS DEFINED ABOVE ####

    }

    /** 
     * main()
     * Usage: Test the functionality of the CardShufflerUI class.
     * Given no parameters, produce the window object
     * Returns Nothing
     * Usage: main()
    */
    public static void main(String[] args) {
        // Main Function
    }
}